{% extends 'base.html.twig' %}

{% block body %}
    <div class="container is-max-desktop">
        <form action="{{ path('support_send') }}" method="post" class="section">
            <h1 class="title"><i class="material-icons" style="font-size: 25px">support_agent</i> Signaler un problème</h1>

            <div class="notification is-info">
                Merci de nous transmettre le plus d'informations possible pour que nous puissions vous aider au mieux.
            </div>

            <div class="field">
                <label class="label">Sujet</label>
                <div class="control has-icons-left">
                    <input class="input" type="text" name="subject" placeholder="Sujet" required>
                    <span class="icon is-small is-left">
                    <i class="material-icons">subject</i>
                </span>
                </div>
            </div>

            <div class="field">
                <label class="label">Message</label>
                <div class="control">
                    <textarea class="textarea" name="message" placeholder="Message" required></textarea>
                </div>
            </div>

            <hr>

            <div class="field">
                <div class="control">
                    <button type="submit" class="button is-link">Envoyer</button>
                </div>
            </div>
        </form>
    </div>
{% endblock %}

